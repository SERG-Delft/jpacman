language: java
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
jdk:
  - oraclejdk8
  - oraclejdk9
jobs:
  include:
    - stage: "Test"
      script: ./gradlew test
    - stage: "Static Analysis"
      script: ./gradlew staticAnalysis
